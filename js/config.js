// -----------------------------------------------------------------------------
// Fill these in after you deploy the Google Apps Script (see README).
// Without apiUrl, the site runs in DEMO mode with sample data (local only).
//
// Site passcode lives in the Google Sheet → Config tab (key: passcode).
// Change it there anytime — no need to edit this file or redeploy Pages.
// The passcode field below is ONLY used in offline demo mode.
// -----------------------------------------------------------------------------
window.CCB_CONFIG = {
  // Web app URL ending in /exec:
  apiUrl: "",

  // Must match TOKEN in google-apps-script/Code.gs
  apiToken: "",

  // Demo-mode only (ignored when apiUrl is set). Prefer Sheet → Config.
  passcode: "",
};
